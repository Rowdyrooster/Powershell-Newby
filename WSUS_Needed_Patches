$output = Get-WsusUpdate -Status Needed, FailedOrNeeded, Failed, NoStatus  | Format-List -Expand EnumOnly
Send-MailMessage -From "WSUS New Patches <Email@email.com>" -to "Email@email.com" -Subject "WSUS Test" -Body ($output | Out-String) -SmtpServer Server.com
